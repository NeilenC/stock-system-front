const EventRow = ({ sector }:any) => {
//     const { events } = useEventsForSector(sector.id);
  
//     return (
//       <Box sx={{ position: "relative", height: "40px", borderBottom: "1px solid #E1E6EF" }}>
//         {events.map(event => (
//           <Box
//             key={event.id}
//             sx={{
//               position: "absolute",
//               left: calculatePosition(event.startDate),
//               width: calculateWidth(event.startDate, event.endDate),
//               height: "100%",
//               backgroundColor: event.color,
//               opacity: 0.8,
//             }}
//           >
//             {event.name}
//           </Box>
//         ))}
//       </Box>
//     );
//   };
  
//   const calculatePosition = (startDate) => {
//     // Lógica para calcular la posición basada en la fecha de inicio
//   };
  
//   const calculateWidth = (startDate, endDate) => {
//     // Lógica para calcular el ancho basado en la duración del evento
//   };
}
  